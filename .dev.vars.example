# 一键部署「Deploy to Cloudflare」会读这个文件，在部署页提示填这些。
# 这里只留唯一必填项，让部署页最简洁。其余可选项（VAPID / APNS_RELAY）不列出——
# 留空时代码会自动兜底（VAPID 首次自动生成存 KV；iOS 走糯叽机默认中转）。
# 想自定义这些可选项 → 部署后在 CF 面板 → Worker → 变量和秘密 手动添加。

# 【必填】手机 App 与本后端的共享密钥。随便一个长随机串（如 32 位字符）。
# 同一个值要填到 App 设置页的「中继密钥」里，两端一致才放行。
RELAY_SECRET=

# 【可选】outbox 结果保留时长（分钟），默认 360（6 小时）。
# 手机离线/没排水超过这个时长，已生成的消息会被清掉 → 丢失。
# 大陆用户常隔很久才开 App，可调更大（如 1440 = 一天）。KV 最低 60s。
# OUTBOX_TTL_MIN=360
